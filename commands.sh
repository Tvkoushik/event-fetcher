sudo apt update
sudo apt install python3
sudo apt install python3-pip
mkdir event-fetcher
chmod 777 event-fecther
cd event-fetcher
create files event.py and requirements.txt as per the code (Eg: vi events.py -> copy the code and paste it inside the VM)
sudo pip3 install -r requirements.txt
sudo apt install firefox